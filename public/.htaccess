# Evita que los usuarios vean el listado de directorios
Options -Indexes

# Configuración del front controller
RewriteEngine On

# Si la petición es para un archivo o directorio existente, no hacer nada
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Para todas las demás peticiones, redirigir a index.php
RewriteRule ^(.*)$ index.php [QSA,L]