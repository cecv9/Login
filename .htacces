# Evita que los usuarios vean el listado de directorios
Options -Indexes

# Redirige todas las peticiones a la carpeta public/
RewriteEngine On
RewriteCond %{REQUEST_URI} !public/
RewriteRule ^(.*)$ public/$1 [L]